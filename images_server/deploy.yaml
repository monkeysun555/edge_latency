#
apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  # annotations:
  #   deployment.kubernetes.io/revision: "1"
  generation: 1
  labels:
    run: echoserver
  name: echoserver
  # namespace: latent
  namespace: latency
  resourceVersion: "1114536"
spec:
  progressDeadlineSeconds: 600
  replicas: 1
  revisionHistoryLimit: 10
  selector:
    matchLabels:
      run: echoserver
  strategy:
    rollingUpdate:
      maxSurge: 25%
      maxUnavailable: 25%
    type: RollingUpdate
  template:
    metadata:
      annotations:
        danm.k8s.io/interfaces: |
          [
            {"network":"default", "ip":"dynamic"},
            {"network":"ipvlan", "ip":"dynamic"}
          ]

      creationTimestamp: null
      labels:
        run: echoserver
    spec:
      containers:
      - args:
        - bash
        image: monkeysun555/centos_server:v1
        imagePullPolicy: Never
        ports:
        - containerPort: 8000
        # command: ["bash", "/etc/rc.start"]
        name: echoserver
        resources: {}
        stdin: true
        terminationMessagePath: /dev/termination-log
        terminationMessagePolicy: File
        tty: true
      dnsPolicy: ClusterFirst
      restartPolicy: Always
      schedulerName: default-scheduler
      securityContext: {}
      terminationGracePeriodSeconds: 30
